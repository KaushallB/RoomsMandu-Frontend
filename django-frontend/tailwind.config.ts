import type { Config } from "tailwindcss";

const config: Config = {
  theme: {
    extend: {
      colors: {
        roomsmandu: "#ff385c",
        "roomsmandu-dark": "#d50027",
      },
    },
  },
};

export default config;
